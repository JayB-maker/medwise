import AuthLayout from "../components/ui/authLayout";
import Reset from "./auths/resetPassword";

const ResetPasswordPage = () => {
  return (
    <AuthLayout>
      <Reset />
    </AuthLayout>
  );
};

export default ResetPasswordPage;
